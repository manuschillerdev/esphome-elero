# ESPHome compile test: ESP32 minimal (no web UI)
# Used to verify core elero component changes
substitutions:
  spi_clk: GPIO18
  spi_mosi: GPIO23
  spi_miso: GPIO19
  spi_cs: GPIO5
  gdo0_pin: GPIO26

esphome:
  name: test-esp32-minimal
  friendly_name: Test ESP32 Minimal

esp32:
  board: esp32dev
  framework:
    type: arduino

logger:
  level: DEBUG

packages:
  common: !include common.yaml
