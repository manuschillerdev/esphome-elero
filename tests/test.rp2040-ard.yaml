# ESPHome compile test: RP2040 with Arduino framework
# Note: elero_web not tested here - async_tcp doesn't support RP2040
substitutions:
  spi_clk: GPIO18
  spi_mosi: GPIO19
  spi_miso: GPIO16
  spi_cs: GPIO17
  gdo0_pin: GPIO20

esphome:
  name: test-rp2040-ard
  friendly_name: Test RP2040 Arduino

rp2040:
  board: rpipicow

logger:
  level: DEBUG

packages:
  common: !include common.yaml
